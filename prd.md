**产品需求文档：校园互助平台**

**一、模块概述**

**模块名称**：校园跑腿互助系统

**所属应用**：微信小程序

**核心目标**：为校园内学生提供安全、便捷、有偿的互助服务（如代取快递、代买、代办等），通过平台担保交易保障双方权益，促进校园资源高效流转。

**用户角色**：发单人（需求方）、接单人（服务提供方，即“骑手”）、平台管理员。

**2. 产品背景与目标**

**• 背景**: 当前校园信息分散在各类微信群、QQ群和公告栏中，信息查找困难，时效性差，且缺乏有效的互动和管理。学生们需要一个统一、便捷、高效的平台来获取信息、表达观点和寻求帮助。

**• 产品目标**:

**◦ 信息聚合**: 整合校园内各类信息，如失物招领、活动通知、兼职信息、二手交易等。

**◦ 促进社交**: 为同校学生提供一个匿名或实名的社交空间，通过共同话题建立连接。

**◦ 构建互助生态**: 建立一个便捷的互助平台（如跑腿），鼓励同学间互相帮助。

**◦ 提升校园生活效率**: 让学生能更快速地解决问题，获取所需资源。

**3. 用户角色**

**角色**

**描述**

**核心需求**

**普通学生 (浏览者)**

大部分用户，主要浏览信息、参与讨论。

快速获取校园热点、寻找答案、围观趣事。

**发帖学生 (贡献者)**

有信息分享、求助或交易需求的学生。

便捷地发布帖子、管理自己的帖子、获得有效回复。

**跑腿用户 (需求方)**

需要他人帮助完成任务（如代取快递）的学生。

快速发布需求、找到可靠的同学接单、安全支付。

**跑腿用户 (接单方)**

有空闲时间并愿意通过帮助他人赚取小额报酬的学生。

方便地查找和接取任务、获得报酬。

**管理员**

负责平台内容审核、用户管理和秩序维护的运营人员。

高效审核内容、处理违规、管理用户和数据分析。

**4. 功能模块详解**

**4.1. 首页 (Home)**

**• 顶部导航栏**:

◦ 左侧: 返回/小程序名称。

◦ 中间: 搜索框，支持关键词搜索帖子、用户。

◦ 右侧: 功能入口（如消息通知）。

**• 快捷功能区**: 图标入口，如“宿舍夜宵”、“互助跑腿”、“论坛群”、“5分打印”等，直达特定服务。

**• Banner广告位**: 轮播图，用于展示平台活动或商业合作广告。

**• 内容分类Tab**: “最新”、“分享吐槽”、“求助答疑”、“组队交友”、“二手闲置”。点击切换不同内容流。

**• 帖子信息流**:

**◦ 置顶帖**: 管理员或付费置顶的帖子，优先展示。

**◦ 普通帖子**: 按时间或热度排序。每个帖子卡片包含：

▪ 用户头像、昵称、发布时间。

▪ 帖子标签 (如 #分享吐槽)。

▪ 帖子标题和内容摘要。

▪ 图片/视频预览（如有）。

▪ 互动区：热度/点赞数、评论按钮、点赞按钮。

**4.2. 热榜 (Hot List)**

**• 入口**: 可在首页或独立Tab进入。

**• 功能**: 展示“金科圈子今日热榜”，按热度（点赞、评论、浏览量综合计算）排名，列出Top 10的热门话题。

**• 目的**: 快速了解校园内大家都在讨论什么，提升用户参与感。

**4.3. 发布 (Post)**

**• 入口**: 底部导航栏中央的“+”号按钮。

**• 发布流程**:

**◦ 选择主题分类**: 弹窗选择帖子类型：分享吐槽、求助答疑、组队交友、二手闲置、树洞。

**◦ 选择校区 (可选)**: 如“江宁校区”、“幕府校区”，用于信息筛选。

**◦ 编辑内容**:

▪ 文本输入框 (限制1000字)。

▪ 图片/视频上传 (最多9张)。

**▪ 联系方式**: 可选择填写，方便他人联系。

**▪ 匿名发布**: 提供“切换第二身份”开关，开启后以“神秘同学+ID”的形式发布。

**▪ 付费置顶**: 提供“发布后置顶”开关（需接入支付功能）。

**◦ 发布规则提示**: 明确告知用户禁止发布重复信息、广告、二维码等内容，违规会被禁言。

**◦ 发布按钮**: 点击后将内容提交至服务器，等待审核（如需要）后发布。

**4.4. 互助 (Mutual Help)**

**• 入口**: 底部导航栏“互助”图标。

**• 功能**: 一个独立的跑腿任务平台。

**• 页面布局**:

**◦ 顶部**: “个人中心”和“发布需求”按钮。

**◦ 订单筛选**: “全部订单”、“未被接单”、“我发布的”、“我帮助的”。

**◦ 任务列表**:

▪ 每个任务卡片包含：发布者昵称、任务描述（如“北区两个小快递送到南9”）、校区标签、到手佣金、任务状态（“待帮助”、“已完成”、“送达待确认”）。

▪ “接单”按钮（仅对未被接单的任务显示）。

**• 发布需求**: 用户填写任务详情、地点、悬赏金额，发布后进入任务列表。

**4.5. 消息 (Messages)**

**• 入口**: 底部导航栏“消息”图标。

**• 功能**: 用户的个人通知中心。

**• 内容**:

**◦ 评论提醒**: 显示谁评论了你的帖子，以及评论内容和原帖摘要。

**◦ 点赞提醒**: 显示谁点赞了你的帖子或评论。

**◦ 系统通知**: 来自管理员的通知，如帖子审核结果、违规警告等。

**◦ 功能**: “一键置为已读”、删除单条消息。

**◦ 顶部引导**: 引导用户关注公众号以开启微信端提醒。

**4.6. 我的 (Profile)**

**• 入口**: 底部导航栏“我的”图标。

**• 功能**: 用户的个人中心。

**• 页面布局**:

**◦ 个人信息区**: 头像、昵称（如“神秘同学2054826543”）、学校名称、已发帖数、被点赞数。

**◦ 功能入口**:

**▪ 内容管理**: “我的帖子”、“历史评论”、“我的蹲贴”、“我的收藏”。

**▪ 收益管理**: “跑腿收益”（仅在互助模块有收益时显示）。

**▪ 服务与支持**: “本校客服”、“官方客服”、“封禁申诉”、“建议反馈”。

**▪ 商务与设置**: “商务合作”、“开通圈子”、“用户协议”、“隐私政策”。

**◦ UID**: 页面底部显示用户唯一ID。

**5. 非功能性需求**

**• 性能**: 页面加载时间不超过2秒，图片加载需有懒加载和占位符。

**• 安全**: 用户手机号等敏感信息需脱敏显示。后台需有完善的内容审核机制，防止违规信息传播。

**• 兼容性**: 适配主流微信版本和不同尺寸的移动设备屏幕。

**• 可用性**: 界面简洁，操作符合用户习惯，新手引导清晰。

**6. 关键用户流程**

**1. 发布一个求助帖**:

1.1. 用户点击底部“+” -> 选择“求助答疑” -> 选择校区 -> 输入标题和内容 -> (可选)上传图片 -> (可选)填写联系方式 -> 点击“发布”。

**2. 发布一个跑腿任务**:

2.1. 用户点击“互助”Tab -> 点击“发布需求” -> 填写任务描述、地点、佣金 -> 发布。

**3. 接取一个跑腿任务**:

3.1. 用户进入“互助”Tab -> 浏览“未被接单”列表 -> 找到一个任务 -> 点击“接单” -> 任务状态变为“待帮助”，并通知发布者。

 

**二、功能流程与页面详情**

**（一）用户认证流程（成为骑手的前提）**

**页面名称**：骑手认证页

**触发条件**：用户点击“互助”模块中的“发布需求”或“接单”时，若未完成认证，则跳转至该页面。

**页面元素**：

**• 顶部提示**：橙色背景文字“完成校园认证和实名认证后，就能申请成为骑手接单赚米了\~”，明确认证目的。

**• 认证步骤**：分3步展示，当前进度高亮（如“1 校园认证 > 2 实名认证 > 3 成为骑手”）。

**◦ 校园认证**：状态“已完成”（绿色标签），说明“上传学生证相关信息，认证你的学生身份”。

**◦ 实名认证**：状态“待完成”（蓝色标签），说明“上传身份证对接权威库认证，后续提现更高效”。

**◦ 联系号码**：显示用户已绑定的手机号（如187\\\*\\\*\\\*\\\*8429），说明“展示给发单同学方便联系”。

**• 操作按钮**：蓝色“完善信息”按钮，点击后引导用户完成实名认证（需调用身份证OCR识别或手动输入+人脸核身）。

**• 底部链接**：“联系客服”，用于解决认证问题。 **业务规则**：

• 仅完成“校园认证+实名认证”的用户，才能发布或接取跑腿订单。

• 联系号码默认读取用户账号绑定手机号，支持修改（需验证新号码）。

**（二）发单流程（需求方）**

**页面1：发布需求页**

**触发条件**：用户点击底部导航“互助”>“发布需求”。

**页面元素**：

**• 顶部提示**：红色警告条“引导您私下交易；引导您主动取消订单，欢迎联系客服举报”，强调平台交易安全。

**• 流程引导**：4步图标说明“1.填写信息 > 2.支付佣金 > 3.接单帮忙 > 4.完成订单”，强化用户对流程的认知。

**• 表单字段**：

**◦ 写明要帮做的事情**（必填）：文本输入框，提示“如去某地领取快递送去某个宿舍、代打印资料…（敏感信息及联系方式此处不要填写）”，限制字数（如200字内），禁止输入手机号、微信等。

**◦ 需要互助的时间**（必填）：时间选择器（精确到小时，如“2026-04-17 20:00前”），支持手动输入。

**◦ 联系号码**（必填）：默认填充用户绑定手机号，支持修改（需验证）。

**◦ 选择校区**（必填）：单选按钮组，选项为“不限校区”“江宁校区”“幕府校区”，默认选中“不限校区”。

**◦ 确认佣金**（必填）：

▪ 输入框：提示“输入你的预算金额\~”，支持手动输入数字（如5元）。

▪ 快捷按钮：预设金额标签（3元、4元、5元、6元、8元、10元），点击后自动填充至输入框。

**◦ 协议勾选**：复选框“我已阅读并同意《逛逛校园圈跑腿平台发单系统协议》”，未勾选时“发布订单”按钮置灰。

**• 底部操作**：

**◦ 悬赏佣金**：实时显示用户输入的佣金金额（如¥0，初始为0，输入后更新）。

**◦ 发布订单**：蓝色按钮，点击后校验表单（必填项、协议勾选），通过后跳转至“支付佣金页”。 **业务规则**：

• 佣金金额需≥平台最低限额（如1元），否则提示“佣金不能低于1元”。

• 发布成功后，订单状态为“未被接单”，进入“互助订单列表”等待接单。

**页面2：支付佣金页**

**触发条件**：发布订单表单校验通过后。

**页面元素**：

• 订单信息预览（需求描述、时间、校区、佣金金额）。

• 支付方式：默认微信支付（小程序内调起支付接口）。

• 支付按钮：点击后扣款，成功后跳转至“订单详情页（发单人视角）”。 **业务规则**：

• 支付成功后，佣金冻结至平台担保账户，订单状态更新为“等待接单”。

• 若支付失败，订单状态保持“未被接单”，用户可重新支付或取消订单。

**（三）订单详情与状态管理**

**页面名称**：订单详情页（发单人视角）

**触发条件**：发单人点击“互助”>“我发布的”订单列表中的某条订单。

**页面元素**：

**• 顶部状态栏**：蓝色背景，显示订单当前状态（如“等待发单人确认”“已完成”“送达待确认”）。

**• 订单信息**：

◦ 待帮助人：显示接单人昵称（如“神秘同学628190”，未接单时显示“等待接单”）。

◦ 订单编号：唯一标识（如e2026041717493264），右侧“复制”按钮，方便用户反馈问题时提供编号。

◦ 下单时间、互助时间、互助地点、订单信息（需求描述）：与发布时填写一致。

**• 仅接单人可见信息**：接单前显示“接单后可查看该信息”（如发单人的具体宿舍号、快递取件码等敏感信息，接单后对骑手可见）。

**• 平台保障说明**：4步图标“1.查看需求 > 2.接单帮忙 > 3.完成订单 > 4.佣金到账”，强化信任。

**• 底部操作**：

◦ 若状态为“送达待确认”：显示“确认完成”按钮，点击后佣金解冻至接单人账户，订单状态更新为“已完成”。

◦ 若状态为“等待接单”：显示“取消订单”按钮，点击后需确认（提示“取消后佣金将原路退回”），取消后订单状态更新为“已取消”。 **业务规则**：

• 接单人完成服务后，需点击“送达”按钮，此时订单状态变为“送达待确认”，发单人需在24小时内确认，超时自动确认。

• 发单人确认完成后，佣金自动结算至接单人钱包（扣除平台服务费，如有）。

**（四）互助订单列表（接单人与发单人共用）**

**页面名称**：互助订单列表页

**触发条件**：用户点击底部导航“互助”。

**页面元素**：

**• 顶部操作栏**：

◦ “个人中心”按钮：跳转至骑手认证/个人中心页。

◦ “发布需求”按钮：跳转至“发布需求页”。

**• 订单筛选标签**：“全部订单”“未被接单”“我发布的”“我帮助的”，默认选中“全部订单”。

**• 订单卡片列表**：

◦ 发单人信息：头像+昵称（如“神秘同学1535122561”）。

◦ 需求描述：如“北区两个小快递送到南9”。

◦ 校区标签：绿色背景“江宁校区”。

◦ 佣金金额：红色“到手佣金 ¥1.70”（显示接单人实际可得金额，若有平台服务费则标注）。

◦ 订单状态：绿色“已完成”、蓝色“送达待确认”、灰色“未被接单”等，不同状态对应不同操作（如“未被接单”时显示“接单”按钮，“已完成”时不可操作）。

**• 底部导航**：与全局一致（首页、互助、发帖、消息、我的）。 **业务规则**：

• 接单人点击“接单”后，订单状态变为“等待发单人确认”，其他用户不可再接单。

• 发单人可在“我发布的”中查看自己发布的所有订单状态，接单人在“我帮助的”中查看自己接取的订单。

**（五）骑手个人中心（接单人视角）**

**页面名称**：骑手个人中心页

**触发条件**：用户点击“互助”>“个人中心”（已完成认证的用户）。

**页面元素**：

**• 用户信息**：昵称（如“神秘同学2054826543”）、头像、实名认证状态（“待完成实名认证”绿色标签）。

**• 数据统计**：

◦ 已接订单：数字（如9），点击可跳转至“我帮助的”订单列表。

◦ 我的钱包：金额（如0.00），点击跳转至“钱包页”。

◦ 已发订单：数字（如0），点击跳转至“我发布的”订单列表。

**• 订单完成率**：绿色进度条“当前订单完成率: 88%”，点击“查看更多”可查看详细完成记录。

**• 功能入口**：

◦ 接单认证：显示认证状态（“待完成实名认证”），点击跳转至“骑手认证页”。

◦ 保证金：显示“当前无需缴纳保证金”（若有保证金规则，此处显示缴纳入口）。

◦ 平台客服：跳转至客服聊天页面。 **业务规则**：

• 订单完成率=（已完成订单数/已接订单总数）×100%，低于80%可能限制接单权限（需平台配置）。

• 钱包金额可提现至微信零钱，需完成实名认证。

**（六）钱包与账单（接单人视角）**

**页面名称**：钱包页

**触发条件**：骑手个人中心点击“我的钱包”。

**页面元素**：

**• 余额信息**：

◦ 可提现余额：大字体显示（如0.00），右侧“提现”按钮，点击后跳转至提现页面（需输入提现金额，调用微信打款接口）。

◦ 本周收入、本月收入、总收入：分别显示对应时间段的佣金收入（如本周0.00、本月0.00、总17.59）。

**• 客服入口**：“跑腿客服”，跳转至客服聊天页面。

**• 账单明细**：

◦ 标签页：“账单明细”（默认）、“提现记录”。

◦ 账单列表：

▪ 收入项：绿色“+”号，如“跑腿收入-e202510112371613 +1.27 余额8.97”，显示订单编号、金额、余额。

▪ 支出项：红色“-”号，如“提现-w2025110108383372 -8.97 余额0.00”“取消跑腿订单手续费-e202510121112469 -0.12 余额7.70”（若用户取消订单，可能扣除少量手续费）。

**▪ 业务规则**：

• 提现需满足最低金额大于0，且每日限提现次数（如3次）。

• 账单明细按时间倒序排列，支持筛选（收入/支出）。

**三、非功能需求**

**• 性能**：订单列表加载时间≤1秒，支付接口响应时间≤2秒。

**• 安全**：

◦ 用户手机号、身份证等敏感信息加密存储，传输过程使用HTTPS。

◦ 支付环节调用微信官方接口，确保资金安全。

◦ 禁止私下交易，系统检测关键词（如“微信”“QQ”）时自动警告或屏蔽。

**• 兼容性**：适配主流微信版本（7.0及以上），支持iOS和Android系统。

 

着重实现的功能

1. 数据层设计：前端状态管理方案

设计要点：

使用 behavior或 composables抽离共享逻辑（如订单状态判断、用户角色校验）。

维护一个全局的 orderStore管理订单列表和状态流转，避免页面间数据不一致。

// stores/orderStore.js

const orderStore = {

  state: {

    orders: \[], // 所有订单

    filters: { type: 'all', campus: '' } // 筛选条件

  },

  // 根据状态更新订单

  updateOrderStatus(orderId, newStatus) {

    const order = this.state.orders.find(item => item.id === orderId);

    if (order) {

      order.status = newStatus;

      this.notify(); // 通知视图更新

    }

  }

};

1. 订单状态机设计与实现

PRD 中定义了完整的状态流转：

待接单 → 进行中 → 送达待确认 → 已完成 / 已取消

设计思路：

用 有限状态机 (Finite State Machine)​ 思想封装一个 statusMachine工具类，管理状态转换规则和对应 UI。

每个状态对应不同的按钮操作和颜色样式。

// utils/statusMachine.js

export const STATUS\_CONFIG = {

  PENDING: { 

    text: '待接单', color: 'gray', 

    actions: \['cancel'] // 发单人可取消

  },

  ACCEPTED: { 

    text: '进行中', color: 'blue', 

    actions: \[] 

  },

  DELIVERED: { 

    text: '送达待确认', color: 'orange', 

    actions: \['confirm'] // 发单人可确认完成

  },

  COMPLETED: { 

    text: '已完成', color: 'green', 

    actions: \[] 

  },

  CANCELLED: { 

    text: '已取消', color: 'grey', 

    actions: \[] 

  }

};

// 检查是否允许状态转换

export function canTransition(from, to) {

  const allowedTransitions = {

    PENDING: \['CANCELLED', 'ACCEPTED'],

    ACCEPTED: \['DELIVERED'],

    DELIVERED: \['COMPLETED']

  };

  return allowedTransitions\[from]?.includes(to) || false;

}

“设计并实现了订单状态机，封装状态转换控制器，统一管理 5 种订单状态的流转逻辑与 UI 映射，确保状态变更的可控性与一致性。”

2. 多角色权限与视图隔离

系统涉及 发单人、接单人、管理员​ 三类角色，同一页面需动态渲染不同内容。

设计思路：

在用户登录后，缓存 userRole（如 sender, rider, admin）。

使用 高阶组件 (HOC)​ 或条件渲染封装权限控制。

// components/RoleGuard.js

function RoleGuard({ role, children }) {

  const userRole = getUserRole();

  return role === userRole ? children : null;

}

// 页面中使用

\<RoleGuard role="rider">

  \<Button>接单\</Button>

\</RoleGuard>

\<RoleGuard role="sender">

  \<Button>取消订单\</Button>

\</RoleGuard>

“实现基于角色的视图控制系统，通过高阶组件统一封装权限校验，支持发单、接单、管理三端在同一页面下的差异化渲染与操作隔离。”

3. 交易闭环模拟（支付与钱包）

即使无法真正对接微信支付，也可以模拟完整流程，展示对业务的理解。

设计思路：

支付流程：下单 → 生成预支付记录 → 模拟扣款 → 更新订单状态。

// services/payment.js

async function mockPayment(orderId, amount) {

  // 1. 生成支付记录（状态：待支付）

  const payRecord = createPayRecord(orderId, amount);

  

  // 2. 模拟调用微信支付（实际项目用 wx.requestPayment）

  const res = await mockWxPay(payRecord);

  

  // 3. 根据结果更新订单与钱包

  if (res.success) {

    await freezeAmount(amount); // 冻结金额

    updateOrderStatus(orderId, 'PENDING');

  }

}

“模拟完整的交易闭环，包括支付预处理、金额冻结、状态同步及异常回滚逻辑，深入理解资金流与订单状态的一致性设计。”

4. 高性能列表与筛选

互助订单列表需支持多维度筛选（全部/我发布的/我帮助的）和分页加载。

设计思路：

使用 防抖 + 分页加载​ 优化频繁筛选请求。

前端维护筛选索引，减少重复计算。

let filterTimer = null;

function handleFilterChange(type) {

  clearTimeout(filterTimer);

  filterTimer = setTimeout(() => {

    loadOrders({ type, page: 1 });

  }, 300);

}

“优化高频交互场景，实现订单列表的动态筛选与分页加载，通过防抖与缓存策略减少重复请求，提升用户体验。”

一、整体架构设计

架构分层图

text

┌─────────────────────────────────────────────────────────┐

│                    视图层 (View Layer)                    │

│  Pages: 首页 | 热榜 | 互助 | 消息 | 我的 | 发布            │

│  Components: OrderCard | RoleGuard | LoadMore ...       │

└─────────────────────────────────────────────────────────┘

                              ↓

┌─────────────────────────────────────────────────────────┐

│                  状态管理层 (Store Layer)                 │

│  orderStore | userStore | walletStore | messageStore    │

│         (响应式数据 + 状态变更通知)                        │

└─────────────────────────────────────────────────────────┘

                              ↓

┌─────────────────────────────────────────────────────────┐

│                   服务层 (Service Layer)                 │

│  API接口定义 | 业务逻辑封装 | 数据转换                     │

└─────────────────────────────────────────────────────────┘

                              ↓

┌─────────────────────────────────────────────────────────┐

│                   数据层 (Data Layer)                    │

│  Mock Data (开发)  ←→  真实API (生产)                    │

│  用户数据 | 订单数据 | 钱包数据 | 帖子数据                  │

└─────────────────────────────────────────────────────────┘

核心设计理念

数据与视图分离：所有业务逻辑放在Service层，页面只负责渲染和交互

 

Mock开关设计：通过配置变量一键切换Mock/真实API

 

状态集中管理：关键业务状态（订单、用户）统一在Store中维护

 

类型安全：使用JSDoc或TypeScript保证数据结构一致性

 

二、完整项目目录结构

text

campus-helper/

├── app.js                          # 小程序入口

├── app.json                        # 全局配置

├── app.wxss                        # 全局样式

├── project.config.json             # 项目配置

├── sitemap.json                    # SEO配置

├── config.js                       # 环境配置（Mock开关）

│

├── pages/                          # 页面目录

│   ├── index/                      # 首页

│   │   ├── index.js

│   │   ├── index.wxml

│   │   ├── index.wxss

│   │   └── index.json

│   ├── hot/                        # 热榜页

│   │   ├── hot.js

│   │   ├── hot.wxml

│   │   ├── hot.wxss

│   │   └── hot.json

│   ├── mutual/                     # 互助模块

│   │   ├── list/                   # 订单列表页

│   │   │   ├── list.js

│   │   │   ├── list.wxml

│   │   │   ├── list.wxss

│   │   │   └── list.json

│   │   ├── detail/                 # 订单详情页

│   │   │   ├── detail.js

│   │   │   ├── detail.wxml

│   │   │   ├── detail.wxss

│   │   │   └── detail.json

│   │   ├── publish/                # 发布需求页

│   │   │   ├── publish.js

│   │   │   ├── publish.wxml

│   │   │   ├── publish.wxss

│   │   │   └── publish.json

│   │   ├── auth/                   # 骑手认证页

│   │   │   ├── auth.js

│   │   │   ├── auth.wxml

│   │   │   ├── auth.wxss

│   │   │   └── auth.json

│   │   └── components/             # 互助模块私有组件

│   │       └── order-card/         # 订单卡片组件

│   ├── post/                       # 发布页

│   │   ├── post.js

│   │   ├── post.wxml

│   │   ├── post.wxss

│   │   └── post.json

│   ├── message/                    # 消息页

│   │   ├── message.js

│   │   ├── message.wxml

│   │   ├── message.wxss

│   │   └── message.json

│   └── profile/                    # 个人中心

│       ├── index/                  # 个人主页

│       ├── wallet/                 # 钱包页

│       ├── order-manage/           # 订单管理（我发布的/我帮助的）

│       └── settings/               # 设置页

│

├── components/                     # 全局组件

│   ├── role-guard/                 # 角色权限组件

│   │   ├── role-guard.js

│   │   ├── role-guard.wxml

│   │   ├── role-guard.wxss

│   │   └── role-guard.json

│   ├── load-more/                  # 加载更多组件

│   ├── empty-state/                # 空状态组件

│   └── status-tag/                 # 状态标签组件

│

├── stores/                         # 状态管理

│   ├── index.js                    # Store入口，统一导出

│   ├── order-store.js              # 订单状态管理

│   ├── user-store.js               # 用户状态管理

│   ├── wallet-store.js             # 钱包状态管理

│   └── message-store.js            # 消息状态管理

│

├── services/                       # 业务服务层

│   ├── index.js                    # 服务统一导出

│   ├── order-service.js            # 订单业务逻辑

│   ├── user-service.js             # 用户业务逻辑

│   ├── wallet-service.js           # 钱包业务逻辑

│   ├── payment-service.js          # 支付业务逻辑

│   └── post-service.js             # 帖子业务逻辑

│

├── mock/                           # 模拟数据

│   ├── index.js                    # Mock数据统一导出

│   ├── user.js                     # 用户模拟数据

│   ├── order.js                    # 订单模拟数据

│   ├── wallet.js                   # 钱包模拟数据

│   ├── post.js                     # 帖子模拟数据

│   └── message.js                  # 消息模拟数据

│

├── utils/                          # 工具函数

│   ├── status-machine.js           # 订单状态机

│   ├── request.js                  # 网络请求封装

│   ├── storage.js                  # 本地存储封装

│   ├── format.js                   # 格式化工具（时间、金额）

│   ├── validator.js                # 表单校验

│   └── constants.js                # 全局常量定义

│

├── behaviors/                      # 复用行为

│   ├── pagination.js               # 分页逻辑

│   └── auth-check.js               # 权限检查

│

└── assets/                         # 静态资源

    ├── images/                     # 图片资源

    ├── icons/                      # 图标资源

    └── styles/                     # 全局样式

        ├── variables.wxss          # CSS变量

        └── common.wxss             # 通用样式
